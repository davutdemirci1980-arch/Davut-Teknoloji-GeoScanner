import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.davutteknoloji.geoscanner",
  appName: "GeoScanner",
  webDir: "dist",
};

export default config;
